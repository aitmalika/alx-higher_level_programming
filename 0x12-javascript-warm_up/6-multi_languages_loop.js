#!/usr/bin/node

// This line specifies this path to the interpreter for the script.

for (const line of ['C is fun', 'Python is cool', 'JavaScript is amazing']) {
  // This loop iterates over each element in this array,
  // ['C is fun', 'Python is cool', 'JavaScript is amazing'].
  // This "line" variable is assigned the value of each element in,
  // this array during each iteration.
  console.log(line);
  // This line outputs thIS value of the "line" variable to the console.
}
